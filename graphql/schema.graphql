type Movie {
    id: Int!
    name: String!
    score: Int!
}

type Query {
    movies: [Movie]!
    movie(id: Int!): Movie
}

type Mutation {
    addMovie(name: String!, score: Int!): Movie!
}

# similar to Postman
# in http://localhost:4000/ graphql playground:
# query {
#   person {
#     name
#     age
#   }
# }

# result:
# {
#   "data": {
#     "person": {
#       "name": "Dokinqs",
#       "age": 24
#     }
#   }
# }